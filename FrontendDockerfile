FROM node:6.11.0

RUN apt install yarn
RUN mkdir -p /usr/src/app

ADD ./frontend/yarn_cache /usr/local/share/.cache/yarn/v1/
ADD ./frontend/package.json ./frontend/yarn.* /tmp/

RUN cd /tmp && yarn
RUN cd /usr/src/app && ln -s /tmp/node_modules

ADD ./frontend /usr/src/app/

WORKDIR /usr/src/app